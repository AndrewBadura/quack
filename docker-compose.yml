services:
  quack:
    image: ghcr.io/andrewbadura/quack:latest
    container_name: quack
    restart: unless-stopped
    
    expose:
      - 3000
      
    networks:
      - npm_default

networks:
  npm_default:
    external: true
